% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotter.R
\name{plot_by_columns}
\alias{plot_by_columns}
\title{Plot columns matching a name pattern against an x variable}
\usage{
plot_by_columns(
  data,
  xvar,
  pattern = NULL,
  varsname = NULL,
  fun = mean,
  na.rm = TRUE,
  fixed = FALSE,
  xlabel = NULL,
  ylabel = NULL,
  legend_labels = NULL,
  title = NULL,
  line = TRUE,
  points = TRUE,
  colors = NULL,
  linetypes = NULL
)
}
\arguments{
\item{data}{A `data.frame`.}

\item{xvar}{Character string. Name of the variable to use on the x-axis.}

\item{pattern}{Character string. Pattern used to select columns. Passed to
`grep()`. For example, `".SE"` selects columns such as `"po.SE"`,
`"ca.SE"`, and `"fu.SE"`.}

\item{fun}{Function used to aggregate values over rows with the same value of
`xvar`. Default is `mean`.}

\item{na.rm}{Logical. Should missing values be removed before aggregation?
Default is `TRUE`.}

\item{fixed}{Logical. Passed to `grep()`. If `TRUE`, `pattern` is matched as
plain text rather than as a regular expression. Default is `TRUE`.}

\item{xlabel}{Optional character string. Label for the x-axis. If `NULL`,
`xvar` is used.}

\item{ylabel}{Optional character string. Label for the y-axis. If `NULL`,
`pattern` is used.}

\item{title}{Optional character string. Plot title.}

\item{line}{Logical. If `TRUE`, lines are drawn. Default is `TRUE`.}

\item{points}{Logical. If `TRUE`, points are drawn. Default is `TRUE`.}
}
\value{
A `ggplot` object.
}
\description{
Plots several numeric columns whose names match a given pattern, such as
`".SE"` or `".p"`, against a selected x variable. Values are aggregated by
the x variable before plotting.
}
\details{
The function identifies all columns whose names match `pattern`, reshapes
them internally to long format, aggregates the values by `xvar` and column
name, and then plots one line per matched column.

This is useful for comparing quantities such as different standard errors,
p-values, or test statistics stored in separate columns.
}
\examples{
\dontrun{
plot_by_columns(results_ubc, xvar = "N", pattern = ".SE")

plot_by_columns(results_ubc, xvar = "rho", pattern = ".p")

plot_by_columns(
  data = results_ubc,
  xvar = "N",
  pattern = ".SE",
  ylabel = "Standard error",
  title = "Estimated SE by sample size"
)
}

}
